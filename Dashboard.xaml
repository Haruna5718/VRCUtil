<Page path="" icon="" name="Dashboard">
    <If data="{vrcutil_modules.length}">
        <True>
            <Box>
                <Horizontal>
                    <Text> Module : {vrcutil_modules.length}</Text>
                    <Space />
                    <Text> OSC : {settings_osc_send}    {vrcutil_osc_receive}</Text>
                </Horizontal>
            </Box>
            <Space />
        </True>
        <False>
            <Space />
            <Horizontal>
                <Space />
                <Vertical>
                    <Text type="title">Welcom to VRCUtil</Text>
                    <Text color="var(--TextFillColorDisabledBrush)">There’s nothing here… at least not yet.</Text>
                </Vertical>
                <Space />
            </Horizontal>
            <Space />
        </False>
    </If>
</Page>