<Page path="" icon="" name="Dashboard">
    <If value="vrcutil.moduleCount">
        <True>
            <Box>
                <Horizontal>
                    <Text> Module : {vrcutil.moduleCount}</Text>
                    <Space />
                    <Text> OSC : {settings.osc.send}    {vrcutil.osc.receive}</Text>
                </Horizontal>
            </Box>
            <Space />
        </True>
        <False>
            <Space />
            <Horizontal>
                <Space />
                <Vertical>
                    <Text type="title">Welcom to VRCUtil</Text>
                    <Text color="var(--TextFillColorDisabledBrush)">There’s nothing here… at least not yet.</Text>
                </Vertical>
                <Space />
            </Horizontal>
            <Space />
        </False>
    </If>
</Page>